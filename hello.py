def hello_world(name: str) -> str:
    """
    Say Hello

    Args:
      name (str): the name to say hello to.
    Returns:
      a string that says "Hello, " followed by the name parameter.
    """
    return f"Hello, {name}"
